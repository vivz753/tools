# LINK IT
# Run `ln -s ~/dev/vivz753/tools/MacOS/zshrc/.zshrc ~/.zshrc`


# This allows you to use * in terminal i.e. git add *.cpp (but sometimes it doesn't work if there are too many folder layers?)
setopt nonomatch
